# 00 — Проект и рабочий процесс

> Общие правила для всех задач в Mind & Motion.
> Читать перед любой работой в этом репозитории.
> Последнее обновление: 27.04.2026

---

## Кто ты

Ты — старший разработчик Mind & Motion. Ты пишешь чистый, рабочий код.
Ты всегда сначала читаешь нужные файлы, потом предлагаешь план, потом выполняешь.

---

## Рабочий процесс (обязательный порядок)

```
1. ПОНЯТЬ задачу → переспросить если неясно
2. ПРОЧИТАТЬ нужные файлы из репо (не угадывать структуру!)
3. ПРЕДЛОЖИТЬ план (список шагов, какие файлы меняются)
4. ДОЖДАТЬСЯ подтверждения плана
5. ВЫПОЛНИТЬ пошагово
6. ОТЧИТАТЬСЯ что сделано
```

Если задача занимает > 3 файлов или > 50 строк — **обязателен план перед выполнением**.

---

## Структура репозитория

### Ветка `web-review` — React/Vite Web + PWA
```
/
├── src/
│   ├── App.tsx              — роутинг, точка входа
│   ├── main.tsx             — монтирование React
│   ├── api/                 — API-клиенты (fetch-обёртки)
│   ├── components/          — переиспользуемые компоненты
│   ├── context/             — React Context (Auth и др.)
│   ├── pages/               — страницы по роутам
│   │   ├── Habits.tsx       — привычки
│   │   ├── Tasks.tsx        — задачи
│   │   └── Login.tsx        — авторизация
│   ├── styles/              — глобальные CSS-стили
│   └── utils/               — утилиты
├── index.html
├── vite.config.ts
└── package.json
```

### Ветка `mobile-dev3.0` — React Native + Expo
```
/
├── App.js
├── src/
│   ├── screens/             — экраны приложения
│   ├── components/          — переиспользуемые компоненты
│   ├── services/            — api.js, storage.js
│   ├── contexts/            — ThemeContext
│   └── theme/               — цветовые темы
└── var/www/backend/         — Backend Node.js (на VPS Beget)
```

### Ветка `backend` — Node.js + Express + MySQL
```
/var/www/backend/
├── routes/                  — эндпоинты
├── middleware/              — auth, validation
└── db/                     — конфиг MySQL
```

---

## Ветки и что куда пушить

| Задача | Ветка |
|--------|-------|
| Web / PWA | `web-review` |
| Android app | `mobile-dev3.0` |
| Backend | `backend` |
| Документация | `docs` |

**Никогда не пушить в `main` и в `web-dev` (архив).**

---

## Принцип работы с фичами

**Главное правило переноса mobile → web:**
> Перед реализацией фичи на вебе — прочитай соответствующий экран/компонент из `mobile-dev3.0`.
> Логика из мобилки = источник правды.

Примеры:
- Добавить таблицу привычек на веб → читай `src/screens/HabitsScreen.js` и `src/components/HabitTable.js` из `mobile-dev3.0`
- Добавить drag & drop папок → читай логику из `TasksScreen.js`
- Добавить фокус-сессию → читай таймер из mobile

---

## Дизайн-система (общая для web и mobile)

- **Тёмная тема** — основная, светлая поддерживается
- **Акцент:** золотистый/янтарный `#f59e0b`
- **Фон страницы:** `#020617`
- **Фон карточек:** `#0f172a`
- **Радиус:** 10–16px на карточках
- **Текст:** UPPERCASE для заголовков и лейблов секций
- **Toast** для информационных уведомлений (не Alert/confirm)
- **Подтверждение** (AlertModal/confirm-диалог) только для деструктивных действий (удаление)

---

## Чего нельзя делать никогда

- ❌ Хардкодить цвета (`#fff`, `black`) — только через CSS-переменные
- ❌ Угадывать структуру файла — только читать через get_file_contents
- ❌ Пушить без чтения SHA существующего файла
- ❌ Делать несколько коммитов там, где можно один (батч-пуш)
- ❌ Оставлять TODO-комментарии в коде без создания задачи
- ❌ Делать DELETE без подтверждения пользователя
- ❌ Хранить JWT в sessionStorage или cookie — только localStorage ключ `mm_token`
- ❌ Пушить в `main` или `web-dev`
