---
tags: [landing, architecture, current-state]
platform: landing
verified: true
verified-date: 2026-07-22
---

# Landing (mindandmotion.store) — текущее состояние

## ✅ Подключён как submodule (2026-07-22)

`landing/` теперь официальный git submodule `E:\ProjectMM\landing`, ветка `landing`, тот же репозиторий `bragingena1998/mindandmotion`. Раньше жил отдельно (см. историю ниже) — это исправлено.

Старая отдельная локальная копия `E:\ProjectMM-landing` (полностью синхронизирована с `origin/landing` на момент подключения, 0 расхождений) избыточна, но оставлена на диске — можно удалить вручную, когда убедишься, что submodule работает как ожидается.

Содержимое (`landing/landing/`):
- `index.html` — ~56 КБ, готовый статический HTML (сгенерирован через v0 by Vercel, см. [[История-решений]] раздел "Лендинг").
- `assets/screens/*.png` — 10 реальных скриншотов приложения (hero-main, hero-left, hero-right, dashboard, habits, tasks, calendar-month, calendar-week, security, logo).
- `info.txt` — пустой (0 байт), заготовка под служебные ссылки (RuStore URL и т.п.), не заполнена.

Деплоится на тот же VPS, отдельный nginx-конфиг `mindandmotion-store`, домен `mindandmotion.store`.

## История (до 2026-07-22)

Ни `mobile/landing` (пустая заготовка — только `assets/screens/`), ни ветка `landing` внутри submodule `mobile` (оказалась случайной полной копией ветки `mobile` — 39000+ файлов Android/Expo-проекта, ошибка создания ветки, описанная в [[История-решений]]) никогда не содержали реального сайта — он существовал только в отдельной, не подключённой к суперпроекту локальной папке. Это расхождение обнаружено и исправлено 2026-07-22.

## Стиль (зафиксированные решения по дизайну)

Тёмный фон `#0f0e0c`/`#0d0d0c`, золотой акцент `#e8b84b`/`#f5c842`, фиолетовый вторичный `#9b7fdb`. Шрифты Unbounded/Manrope (заголовки) + Inter (текст). Порядок секций: Hero → Offline (как дифференциатор) → Дашборд → Задачи → Привычки → Календарь → Защита → CTA.
