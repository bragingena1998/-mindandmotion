# 🚀 ЖУРНАЛ РАЗРАБОТКИ: MINDANDMOTION (WEB + MOBILE)

## 📊 ОБЩАЯ ИНФОРМАЦИЯ
- **Проект:** Mind & Motion — привычки/задачи/продуктивность
- **Старт миграции в mobile:** 02.02.2026
- **Разработчик:** Гена
- **Статус:** 🔄 Активная разработка (без остановки production)

---

## 🎨 МОЙ СТИЛЬ РАБОТЫ (ВАЖНО!)
### ✅ Что работает для меня:
- Пошагово, с командами и точными файлами
- По выводу консоли/логам
- На русском (комментарии/объяснения)
- Сразу тестировать после каждого изменения

### ❌ Что НЕ работает:
- Абстрактные советы без конкретики
- Пропуск шагов
- “погугли сам”

---

## 🏗️ АРХИТЕКТУРА

### Backend (PRODUCTION)
- Node.js + Express + MySQL
- JWT авторизация
- Основные таблицы: users, habits, habit_records (+ новые: habit_monthly_configs)
- Прод работает отдельно от mobile-ветки (код лежит в `/var/www/backend`)

### Frontend WEB (PRODUCTION)
- mindandmotion.ru (старый интерфейс)

### Frontend MOBILE (DEVELOPMENT)
- Expo + React Native Web (локальная разработка)
- Ветка: `mobile-dev`

---

## 🎯 СТРАТЕГИЯ РАЗРАБОТКИ (БЕЗ РИСКОВ!)
### Фаза 1: Параллельная разработка
- Production живёт своей жизнью
- Mobile версия разрабатывается локально

### Фаза 2: Тестирование
- Тестируем локально (web) и на телефоне (Expo Go)

### Фаза 3: Переход
- Позже: деплой mobile/web-версии на поддомен, постепенная миграция пользователей

---

## 📅 ДОРОЖНАЯ КАРТА (актуальная)

### ✅ ЭТАП 0: Подготовка
- Стратегия миграции, журнал, репозиторий

### ✅ ЭТАП 1: Окружение Expo + RN Web
- Проект поднят, веб/моб запускаются

### ✅ ЭТАП 2: API + Авторизация + Навигация
- Axios client, токен-хранилище, логин/выход, базовая навигация

### ✅ ЭТАП 3: UI база + темы (частично/в процессе)
- Базовые компоненты (кнопки/модалки), тема/цвета

### ✅ ЭТАП 4: Привычки (КРУПНЫЙ ПРОГРЕСС)
Сделано:
- Таблица привычек и записи по дням
- Выбор месяца (кликабельная дата + MonthPickerModal)
- Автоскролл к сегодняшнему дню (исправлен на мобилке)
- Сортировка привычек:
  - Mobile: drag & drop
  - Web: кнопки ↑/↓
  - Сохранение порядка в БД (order_index)
- “История не ломается”:
  - Появилась таблица `habit_monthly_configs`
  - План/единица измерения теперь могут быть разными по месяцам
  - Архивация привычки на конкретный месяц
- “Новая привычка не появляется в прошлом”:
  - habits.start_year + habits.start_month
  - backend фильтрует вывод по запрашиваемому периоду
- Фиксы крашей:
  - защита от `null unit` в HabitTable

Следующий шаг по привычкам:
- “Удалить навсегда” vs “Архив навсегда”: продумать отдельный флаг для скрытия в будущем, но сохранения в истории
- Оптимизация запросов (кэширование, pagination при больших данных)

### ⏳ ЭТАП 5: Задачи
- перенести функционал задач в mobile

### ⏳ ЭТАП 6: Профиль/настройки
- профиль, настройки, смена пароля и т.д.

### ⏳ ЭТАП 7: Тестирование/полировка
- проверка на Android/iOS/Web, багфиксы

### ⏳ ЭТАП 8: Деплой
- поддомен, сборки, плавная миграция

---

## 📝 СЕССИИ РАБОТЫ

### 📅 12.02.2026 — Сессия: Привычки 2.0 (история + порядок + месяцы)
- Сортировка привычек: Drag (mobile) + ↑/↓ (web) + сохранение order_index
- Выбор месяца через модалку, подгрузка данных по периоду
- Переход на помесячные настройки привычек (habit_monthly_configs)
- start_year/start_month для “не показывать в прошлом”
- Исправления автоскролла и крашей таблицы

---

## 🐛 БАГИ И ПРОБЛЕМЫ
### 🔴 Критичные:
- —

### 🟡 Важные:
- Проверить сценарии архивации/удаления при большом числе привычек

### 🟢 Мелкие:
- убрать/подавить warning-и библиотек на Web (React19/ref warnings)

---

## 💡 ИДЕИ И УЛУЧШЕНИЯ
- Графики прогресса по месяцам
- Архив привычек “в будущем” отдельной кнопкой
- Экспорт отчётов (CSV)

---

## 📌 ТЕКУЩИЙ ФОКУС
- Синхронизировать production backend в GitHub (main)
- Довести логику “удалить навсегда/архив навсегда” до понятной UX-модели

- ### 📅 12.02.2026 — Сессия: Задачи 2.0 (Статистика + Подзадачи + Архив)
**Статус:** ✅ Успешно

**Сделано:**
1.  **UX/UI Задач:**
    - Убраны лишние кнопки, чистый дизайн.
    - Тап на задачу → раскрытие подзадач.
    - Тап на чекбокс → выполнение.
    - Долгий тап → меню редактирования/удаления.
    - FAB (кнопка +) поднята над таббаром.

2.  **Подзадачи (Subtasks):**
    - Создана таблица `subtasks` (BIGINT fix).
    - API: CRUD для подзадач.
    - UI: Аккордеон, добавление, удаление, чекбоксы внутри карточки.

3.  **Статистика:**
    - Перенесена на сервер (`/api/tasks/stats`).
    - Считает: Сегодня (План/Факт), Неделя, Месяц, Всего.
    - Исправлен баг с часовыми поясами (учет `done_date`).
    - Исправлен баг "пропадающих" выполненных задач (восстановлены `done_date` в БД).

4.  **Архив:**
    - Фильтрация по месяцам.
    - Возможность смотреть историю задач за прошлые периоды.

**Следующий шаг:**
- Вкладка "Профиль"
- Дизайн вкладки "Привычки"
### 📅 13.02.2026 (Утро) — Редизайн Привычек и Профиля
**Статус:** ⚠️ Блокер на Бэкенде (Профиль)

**Сделано (Frontend):**
1.  **HabitsScreen:**
    - Новый дизайн: Лаконичные LifeProgressBars, цитата дня, виджет "Сегодня".
    - Логика расчета жизни: Добавлена зависимость от пола (М=67, Ж=78.5).
    - Календарь: Исправлен CSS (белый текст).
2.  **ProfileScreen:**
    - Полноценный UI: Аватар (Инициал), Email, Дата регистрации.
    - Настройки: Смена темы, Переключатель пола (М/Ж).
    - Смена пароля: Модальное окно + валидация.

**Проблема (Backend):**
- Сервер не отдает данные профиля (`gender`, `birthdate`) и не обрабатывает смену пароля.
- Роуты `/api/user/profile` и `/api/user/password` добавлены в код, но по какой-то причине не работают (404/500) или игнорируются.
- Есть подозрение на конфликт версий `server.js` или кеширование PM2.

**План:**
1.  Изучить репозиторий через GitHub Connector.
2.  Найти актуальный `server.js` и проверить наличие роутов.
3.  Починить API Профиля.
4.  Связать Фронт и Бэк (чтобы бары ожили).

📅 14.02.2026 — 16.02.2026 — Сессия: Финальная битва за Mobile Production
Статус: ✅ ПОБЕДА (APK собран и работает)

Проблемы (было больно):

Рассинхрон Сервера:

Код обновляли в mindandmotion-mobile, а сервер запускался из старой папки /var/www/backend.

Решение: Перенесли актуальный server.js (с чатом и ГМО) в папку бэкенда.

Сетевой Ад (Android):

Приложение не видело сервер (Network Error, undefined body).

Android блокировал HTTP трафик.

Решение:

В server.js подняли express.json() на самый верх (чтобы body парсился до логов).

В app.json включили usesCleartextTraffic для Android.

Добавили middleware-шпиона в логи, чтобы видеть каждый чих.

Путаница с API путями:

Приложение стучалось в /auth/login, а сервер ждал /api/login.

В api.js то добавляли, то убирали /api.

Решение:

Зафиксировали BASE_URL = 'http://85.198.96.149:5000/api'.

Привели все роуты в приложении к единому виду (убрали лишние /api в вызовах).

UI Баги:

Белый фон съедал половину экрана на логине.

Решение: Переписали Background.js (абсолютное позиционирование, width: 100%, темная подложка).

Сделано (Функционал):

Тайный Чат (Secret Chat):

Реализован вход по кодовой фразе ("семечка сомнения", "попка").

ГМО-режим: искажение текста (Zalgo, перевертыши) на сервере.

Логика "Призрака": сообщения исчезают, ники меняются.

UI: Темная тема, анимации, автоскролл.

Регистрация и Вход:

Работает создание пользователя.

Работает вход (JWT сохраняется).

Восстановление пароля (отправка на Email починена на сервере).

Профиль:

Подгружается пол (влияет на LifeProgress).

Смена пароля работает.

Привычки и Задачи:

Полностью синхронизированы с БД.

Отображаются корректно.

Итог:
У нас есть полностью рабочий APK, который можно ставить на телефон. Сервер работает стабильно. База данных одна и для Web, и для Mobile.


