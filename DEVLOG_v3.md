# 📋 DEVLOG v3.0 — Журнал разработки

> **Версия:** 3.0 (Mind&Motion Mobile)
> **Рабочая ветка:** `mobile-dev3.0`
> **Старт разработки:** 14.03.2026
> **Цель:** Глобальный реворк архитектуры задач, календаря, дашборд, онбординг, брендинг

---

## 📅 14.03.2026 (суббота)

### ✅ Сделано
- Удалена неудавшаяся ветка `mobile-dev3.0` (предыдущая попытка)
- Создана чистая ветка `mobile-dev3.0` от `mobile-dev` (v2.0)
- Проведён полный анализ проекта: код, БД, документация v2.0
- Согласован полный план разработки (11 этапов)
- Созданы документы `ROADMAP_v3.md` и `DEVLOG_v3.md`
- Выполнены миграции БД MySQL: добавлены поля `subtasks_count` (tasks), `emoji` (folders), `type` + `notify_before` (birthdays)
- Актуальный `server.js` и файлы бэкенда залиты в ветку

### 🔄 TimePicker v1
- Создан компонент с бесконечным ScrollView (LOOP_COUNT=100)
- Проблема: 12 000 DOM-узлов → зависание на 30-60 сек при открытии модалки

### 📋 Контекст проекта
- **Сервер:** Node.js + Express на Beget (`var/www/backend`)
- **БД:** MySQL на Beget, управление через phpMyAdmin
- **Мобилка:** React Native + Expo, тестирование через Dev Client на Android
- **Уведомления:** Expo Notifications — работают только в собранном APK
- **Логотип:** золотой треугольник на тёмном фоне (готов)

---

## 📅 15.03.2026 (воскресенье)

### ✅ Сделано

#### ⚡ TimePicker — полный реворк производительности
- `ScrollView` заменён на `FlatList` с `getItemLayout` — рендерится только видимое окно (~10 элементов)
- Множитель уменьшен с `×200` до `×40` (итого 960 и 2400 элементов против 2 880 и 12 000)
- Убран `initialScrollIndex` — заменён на `onLayout` + `scrollToOffset` с точным пикселем
- Добавлен `disableIntervalMomentum={true}` — список всегда останавливается строго на ближайшей позиции
- Результат: открытие мгновенное, защёлкивание работает корректно
- ⚠️ Небольшие шероховатости при очень быстром броске — оставлено в техдолг, не критично

#### 📅 DatePicker — нативный
- Заменён кастомный компонент на `react-native-modal-datetime-picker`
- Вызывает системный Android DatePicker с локализацией `ru_RU`
- Кнопка ✕ для сброса deadline через проп `allowClear`

#### 🔄 Фикс deadline у цикличных задач
- Раньше: `deadline = newTask.deadline || newTask.date` → дата начала всегда уходила как дедлайн
- Теперь: `deadline: newTask.deadline || null` → сервер получает `null` если не выбрано
- Следующая итерация цикличной задачи больше не получает «случайный» дедлайн

#### 🗂️ Доп. настройки скрыты по умолчанию
- `showAdvancedSettings = false` при открытии новой задачи через FAB
- При редактировании — раскрываются автоматически только если есть время / deadline / повторение

#### 📜 Фикс скролла в модалке
- Проблема: `TouchableOpacity` / `TouchableWithoutFeedback` перехватывали gesture-события у вложенных ScrollView/FlatList
- Решение v1: `TouchableWithoutFeedback` — помогло частично (первый скролл работал, повторные лагали)
- Решение v2 (финальное): `Pressable` — не захватывает responder у дочерних компонентов, скролл стабилен
- `nestedScrollEnabled` на ScrollView модалки для надёжности

#### 🗂️ Этап 2: Папки — полная реализация
- API-слой: `foldersAPI` (get/create/update/delete/reorder) — всё в `TasksScreen.js`
- UI: горизонтальный ScrollView чипов. Тап = фильтр, долгое нажатие = меню действий
- Меню папки: переместить влево/вправо, редактировать, удалить
- Drag-and-drop: `PanGestureHandler` + `LongPressGestureHandler` (pan снаружи, longPress внутри)
- Призрак задачи под пальцем во время drag
- Анимированный пульс у папок-зон при наведении (`FolderDropChip` с `Animated.loop`)
- Выбор папки при создании/редактировании задачи в раскрывашке «Дополнительно»
- `folderId` корректно передаётся на сервер через все CRUD-операции
- Toast-уведомления при всех ключевых действиях

#### 🐛 Багфиксы в рамках Этапа 2
- **Drop не работал** — координаты чипов не успевали записаться до первого drag. Решение: `setTimeout(..., 50)` + перезамер `foldersRowRef` в момент старта drag
- **Зона попадания** расширена с ±10px до ±20px по вертикали
- **Анимация hover** была через `transform.scale` в state — теперь отдельный `FolderDropChip` с собственным `Animated.Value` и `Animated.loop`

### 🐛 Известные проблемы / Техдолг
- TimePicker: небольшой дребезг при очень быстром броске (не критично, оставлено на потом)
- Modal scroll: изредка чуть медленнее при первом касании на старых Android (наблюдаем)

---

## 📅 17.03.2026 (вторник)

### ✅ Сделано — Этап 3: Привычки, Багфиксы и UX

#### 🗑️ Багфикс удаления привычек
- Удаление не срабатывало из-за отсутствия обработчика в `HabitTable.js`
- Добавлен проп `onHabitDelete` → передаётся в `HabitsScreen.js` → вызывает `confirmDeleteHabit`
- Реализован модал подтверждения удаления с `AlertModal`-стилем через кастомный `Modal.js`
- Оптимистичный UI: привычка и её записи удаляются из state до ответа сервера

#### 📋 Разгрузка формы создания привычки
- Поля «Начало», «Конец», «Дни недели» скрыты в раскрывашку «▶ Дополнительные настройки»
- При редактировании — раскрываются автоматически если есть данные
- Форма теперь выглядит компактно при создании новой привычки

#### 📐 Выравнивание поля «План» и тоггла
- Проблема: `Input.js` имеет `marginBottom: 16` в styles.container — кнопка тоггла сдвигалась вниз
- Решение: проп `containerStyle={{ marginBottom: 0 }}` на `<Input>` в строке планирования
- Тоггл-кнопка имеет фиксированную `height: 48` — встаёт в одну линию с полем ввода

#### 🔘 Тоггл «в день / за период»
- Заменён с `Button`-компонента на кастомный `TouchableOpacity` (`targetToggleBtn`)
- Стиль: скруглённый прямоугольник, акцентный фон при выборе «за период»
- Текст кнопки динамически показывает диапазон дат если он выбран: «за 01.04–30.04»

#### 💬 Тексты подсказок
- `в день` → «Количество в день»
- `за период` → «Суммарное количество за весь выбранный период»

#### 🔴 Кнопка «Удалить» — фикс внешнего вида
- Проблема 1: двойная рамка (зелёная поверх красного градиента) — в `Button.js` `getBorderStyle()` возвращал `borderWidth: 1, borderColor: accentBorder` для всех вариантов включая `danger`
- Решение: добавлен проп `noBorder` в `Button.js` → `getBorderStyle()` возвращает `{ borderWidth: 0 }`
- Проблема 2: текст кнопки был чёрным на красном фоне (невидимо) — в `getTextColor()` отсутствовал `case 'danger'`
- Решение: добавлен `case 'danger': return '#FFFFFF'`
- Проблема 3: фон кнопки был чёрным — `LinearGradient` получал `[danger1, undefined]` т.к. `danger2` не определён в `ThemeContext.js`
- Решение: `colors.danger2 ?? colors.danger1` — используем `danger1` как fallback
- Итог: кнопка «Удалить» — красная, белый текст, без лишней рамки ✅

### 🐛 Баги / Проблемы
- Все баги этапа 3 исправлены в рамках сессии

---

## 📅 20.03.2026 (четверг)

### ✅ Сделано — Этап 5Г: Интеграция уведомлений в UI

#### 🔗 TasksScreen.js — уведомления для задач
- Добавлен импорт `scheduleTaskReminders`, `cancelTaskReminders`
- **Создание/редактирование задачи:** вызов `scheduleTaskReminders()` если есть `time` и `date`
- **Удаление задачи:** вызов `cancelTaskReminders()` для отмены запланированных уведомлений
- Оптимистичный UI сохранён — уведомления планируются после успешного API-запроса

#### 📅 CalendarScreen.js — уведомления для ДР/событий
- Добавлен импорт `scheduleBirthdayNotification`, `cancelBirthdayNotification`
- **Создание события:** вызов `scheduleBirthdayNotification()` с результатом от API
- **Редактирование события:** обновление уведомления через `scheduleBirthdayNotification()`
- **Удаление события:** вызов `cancelBirthdayNotification()` для отмены

#### 🔔 NotificationSettingsScreen.js — перепланирование при изменении
- Добавлен импорт `rescheduleAllDailyNotifications`
- **Автоперепланирование:** при изменении времени или включения утренних/вечерних/недельных уведомлений
- Настройки сохраняются мгновенно, уведомления перепланируются сразу

#### 🎯 FocusSessionModal.js — уведомление о конце сессии
- Добавлен импорт `scheduleSessionEndNotification`, `cancelSessionEndNotification`
- **Старт таймера:** планирование уведомления о конце сессии
- **Пауза/стоп/сброс:** отмена запланированного уведомления
- Интеграция с существующей логикой вибрации и UI

#### 📋 Документация
- Обновлён `ROADMAP_v3.md` — Этап 5Г отмечен как завершён
- Этап 5 переведён в статус «Завершён» с датой 20.03.2026

### 🔄 Следующий шаг
- **Этап 5Д** — Foreground Service для Android (постоянное уведомление таймера)
- **Тестирование** всех уведомлений через собранный APK

---

## 📅 18.03.2026 (среда)

### ✅ Сделано — Этап 4: Календарь — Глобальное обновление

#### 📅 Недельный вид
- Горизонтальная полоса с 7 днями текущей недели (Пн–Вс)
- Активный день подсвечен акцентным цветом, текущий день выделен отдельно
- Горизонтальный свайп (`PanGestureHandler`) для смены недели вперёд/назад

#### 📊 Переключатель вида
- Dropdown в шапке: «МАРТ 2026 Г. ▼» — тап открывает выбор режима
- Режимы: недельный / месячный
- Месячная сетка — стандартный календарный вид

#### 🔵 Индикаторы на днях
- Точки под числами: акцентный = задачи, зелёный = привычки, фиолетовый = события/ДР
- Корректный подсчёт `active_habits` по каждому дню (не суммарный за месяц)
- Фикс `month` параметра: клиент передаёт 1-based, убран двойной `+1` на сервере

#### 📋 День-панель (DayPanel)
- Bottom sheet при тапе на день в календаре
- Секции: «Задачи» / «Привычки» / «События» — пустые скрыты
- Выполненные задачи зачёркнуты
- `ScrollView` внутри панели не конфликтует со свайпом недели
- Фикс layout: `flex: 1` на flex-контейнере, корректная высота панели

#### 🎂 ДР и личные события
- Поле `type` (birthday / event / reminder) и `notify_before` добавлены в БД
- Форма создания: барабанные пикеры дня/месяца/года, тип события, заметки
- Отображаются в день-панели в секции «События»

#### 🐛 Багфиксы в рамках Этапа 4
- **Свайп недели конфликтовал со скроллом DayPanel** — решено через `activeOffsetX` на `PanGestureHandler`
- **Индикатор привычек** показывал суммарное число вместо per-day — фикс в запросе к API
- **month off-by-one** — убран лишний `+1` в параметре запроса задач по месяцу
- **modal(swipe-to-close)** — добавлен жест закрытия модалки свайпом вниз (`aa6ae028`)

### 🔄 Следующий шаг
- **Этап 5:** Уведомления — Тонкая настройка
  - Экран настройки: утреннее/вечернее напоминание, ДР/события
  - Напоминание за 10 минут до задачи с установленным временем
  - Убрать тестовую кнопку уведомления из Профиля

---

## 📝 Шаблон записи

```
## 📅 ДД.ММ.ГГГГ (день недели)

### ✅ Сделано
-

### 🐛 Баги / Проблемы
-

### 🔄 В процессе
-

### 📝 Заметки
-

---

## 📅 20.03.2026 (пятница)

### ✅ Сделано — Этап 6: Онбординг и Демо-данные (полная реализация)

#### 🎯 Демо-данные для новых пользователей
**`var/www/backend/utils/demoData.js`** — скрипт генерации демо-данных:
- **6 задач** — разные приоритеты (высокий/средний/низкий), даты (сегодня/завтра/вчера), типы (обычные, цикличные, просроченные)
- **6 привычек** — разные частоты (daily/weekly), прогресс за 30 дней, лучшие streak
- **5 событий** — ДР, события, важные даты с напоминаниями
- **Автоматическая генерация** записей привычек за последние 30 дней (80% вероятность выполнения)
- **Интеграция** в `auth.js` — вызов `createDemoData()` после успешной регистрации

#### 📚 Overlay-туториалы
**`src/components/TutorialOverlay.js`** — базовый компонент:
- Spotlight эффект с анимированным появлением
- Поддержка указателей жестов (тап, свайп)
- Прогресс-бар с точками для навигации
- Кнопки «Назад/Далее/Пропустить/Завершить»

**`src/hooks/useTutorial.js`** — хук управления:
- Сохранение завершенных туториалов в AsyncStorage
- Функции старта, перезапуска, пропуска
- Автоматический запуск при первом входе

**`src/components/TutorialButton.js`** — кнопка "?":
- Стилизованная кнопка в правом верхнем углу
- Тень и анимации для лучшего UX

#### 🎮 Туториалы по экранам
**`src/components/TasksTutorial.js`** — 5 шагов:
1. Приветствие и обзор экрана
2. Создание задачи (кнопка +)
3. Фильтрация задач
4. Свайпы для действий (выполнение/фокус)
5. Завершение и призыв к действию

**`src/components/HabitsTutorial.js`** — 6 шагов:
1. Приветствие в мире привычек
2. Создание привычки
3. Отметка выполнения
4. Статистика и прогресс
5. Цепочки успеха (streak)
6. Завершение

**`src/components/CalendarTutorial.js`** — 6 шагов:
1. Приветствие в календаре
2. Навигация по месяцам
3. Просмотр дня (DayPanel)
4. Создание событий
5. Индикаторы активности
6. Завершение

#### 🔧 Интеграция в экраны
**`src/screens/TasksScreen.js`**:
- Автозапуск туториала при первом входе с демо-данными
- Кнопка "?" для перезапуска
- Интеграция с существующим UI

**`src/screens/HabitsScreen.js`**:
- Автозапуск при наличии привычек
- Кнопка "?" для перезапуска
- Адаптация под существующий layout

**`src/screens/CalendarScreen.js`**:
- Автозапуск при наличии событий
- Кнопка "?" для перезапуска
- Совместимость с gesture handlers

#### 📋 Документация
- Обновлён `ROADMAP_v3.md` — Этап 6 отмечен как завершён
- Детальное описание всех компонентов и функций
- Статус проекта: 6 из 11 этапов завершены

### 🔄 Следующий шаг
- **Этап 7** — Дашборд (главный экран со сводкой)
- **Этап 5Д** — Foreground Service для Android (оставшийся техдолг)

---

## 📅 21.03.2026 (суббота)

### ✅ Этапы 9 + 10 (без дублирования логотипа на экранах)

#### Этап 9 — Безопасность
- `src/services/appLock.js` — PIN (4–6 цифр), SHA-256 + соль, `expo-secure-store`; флаги в AsyncStorage
- `src/screens/AppLockScreen.js` — клавиатура, биометрия (`expo-local-authentication`), авто-запрос Face ID/отпечатка при включённой опции
- `src/components/AppLockSettingsSection.js` — блок «Защита приложения» в настройках: PIN, биометрия, смена/отключение
- `App.js` — после логина / при холодном старте: экран блокировки если PIN включён; `AppState` → блокировка при уходе в фон
- iOS: `NSFaceIDUsageDescription` в `app.json`

#### Этап 10 — Брендинг
- `src/components/BrandedSplash.js` — тот же SVG-логотип что у таба дашборда + анимированный «блик»
- Старт: нативный splash сразу скрывается (`expo-splash-screen`), ~1.1 с показывается `BrandedSplash`; фон splash в `app.json` → `#0f0f11`
- Мини-логотипы по углам экранов **не** добавлялись — бренд уже в центральной кнопке табов

#### Зависимости
- `expo-splash-screen`, `expo-local-authentication`, `expo-crypto`, `expo-secure-store`

---
