# docs-v2 — свёрнуто в vault

Эта папка была черновиком новой документации, начатым до появления Obsidian-vault.
С 2026-07-22 вся документация, история решений и известные проблемы ведутся в
`E:\ProjectMM\vault` (git-подмодуль, ветка `docs-vault`) — см. `vault/00-Обзор/Обзор.md`.

`backlog.md` (аспирационный план аудита) и `history-decisions.md` (анализ архива
`context-perplexity/`) перенесены и раздроблены на атомарные заметки в
`vault/08-Решения/` и `vault/09-Проблемы/`, дополнены свежим аудитом реального кода.

Эта папка оставлена пустой намеренно — не использовать для новых заметок, писать в vault.
